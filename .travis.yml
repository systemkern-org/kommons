language: java

before_install:
  - chmod +x gradlew
  - echo $GPG_SECRET_KEYS | base64 --decode | $GPG_EXECUTABLE --import
  - echo $GPG_OWNERTRUST | base64 --decode | $GPG_EXECUTABLE --import-ownertrust
  - $GPG_EXECUTABLE --keyring secring.gpg --export-secret-keys > ~/.gnupg/secring.gpg
  - ls ~/

install: ./gradlew build

cache:
  directories:
    - ~/.m2/repository

deploy:
  - provider: script
    script: ./gradlew uploadArchives
    on:
      tags: true
